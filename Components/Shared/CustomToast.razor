@if (IsVisible)
{
    <div class="custom-toast @ToastTypeClass @(IsVisible ? "show" : "")">
        <div class="toast-icon">
            @if (ToastType == ToastType.Success)
            {
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
                    <polyline points="22 4 12 14.01 9 11.01"></polyline>
                </svg>
            }
            else if (ToastType == ToastType.Error)
            {
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <circle cx="12" cy="12" r="10"></circle>
                    <line x1="15" y1="9" x2="9" y2="15"></line>
                    <line x1="9" y1="9" x2="15" y2="15"></line>
                </svg>
            }
            else if (ToastType == ToastType.Info)
            {
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <circle cx="12" cy="12" r="10"></circle>
                    <line x1="12" y1="16" x2="12" y2="12"></line>
                    <line x1="12" y1="8" x2="12.01" y2="8"></line>
                </svg>
            }
        </div>
        <div class="toast-content">
            <div class="toast-message">@Message</div>
        </div>
        <button class="toast-close" @onclick="Close">
            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <line x1="18" y1="6" x2="6" y2="18"></line>
                <line x1="6" y1="6" x2="18" y2="18"></line>
            </svg>
        </button>
    </div>
}

@code {
    [Parameter] public string Message { get; set; } = string.Empty;
    [Parameter] public ToastType ToastType { get; set; } = ToastType.Info;
    [Parameter] public int Duration { get; set; } = 5000;
    [Parameter] public EventCallback OnClose { get; set; }

    private bool IsVisible { get; set; }
    private string ToastTypeClass => ToastType.ToString().ToLower();

    protected override async Task OnParametersSetAsync()
    {
        if (!string.IsNullOrEmpty(Message))
        {
            IsVisible = true;
            StateHasChanged();

            await Task.Delay(Duration);
            await Close();
        }
    }

    private async Task Close()
    {
        IsVisible = false;
        StateHasChanged();
        await OnClose.InvokeAsync();
    }
}
